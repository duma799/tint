name: Release

# Every pull request and push builds the Mac files (attached to the run, for
# trying out) in a read-only job. A push to main whose version
# (Directory.Build.props) has no release yet then publishes them as release
# vX.Y.Z and updates the Homebrew formula and cask — in a separate job that
# never runs pull-request code.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  package:
    runs-on: macos-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Version
        id: version
        run: echo "version=$(sed -n 's:.*<Version>\(.*\)</Version>.*:\1:p' Directory.Build.props)" >> "$GITHUB_OUTPUT"

      - name: Package for Apple silicon
        run: scripts/package.sh osx-arm64

      - name: Package for Intel
        run: scripts/package.sh osx-x64

      - name: Smoke test
        run: |
          mkdir -p "$RUNNER_TEMP/try"
          tar -xzf dist/tint-*-osx-arm64.tar.gz -C "$RUNNER_TEMP/try"
          "$RUNNER_TEMP/try/tint" --version
          "$RUNNER_TEMP/try/tint" palette assets/icon.png
          "$RUNNER_TEMP/try/tint" apply assets/icon.png --no-reload
          ditto -x -k dist/Tint-*-osx-arm64.zip "$RUNNER_TEMP/try"
          codesign --verify --strict --verbose "$RUNNER_TEMP/try/Tint.app"
          test -x "$RUNNER_TEMP/try/Tint.app/Contents/MacOS/Tint"

      - uses: actions/upload-artifact@v4
        with:
          name: tint-${{ steps.version.outputs.version }}-mac
          path: dist/

  publish:
    needs: package
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
      VERSION: ${{ needs.package.outputs.version }}
    steps:
      - uses: actions/checkout@v5

      - name: Already released?
        id: check
        run: |
          if gh release view "v$VERSION" > /dev/null 2>&1; then
            echo "released=true" >> "$GITHUB_OUTPUT"
            echo "v$VERSION is already released; nothing to do."
          else
            echo "released=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/download-artifact@v4
        if: steps.check.outputs.released == 'false'
        with:
          name: tint-${{ needs.package.outputs.version }}-mac
          path: dist/

      - name: Publish the release
        if: steps.check.outputs.released == 'false'
        run: gh release create "v$VERSION" dist/* --target "$GITHUB_SHA" --title "tint $VERSION" --generate-notes

      - name: Update the Homebrew formula and cask
        if: steps.check.outputs.released == 'false'
        run: |
          scripts/homebrew.sh "$VERSION"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula Casks
          git commit -m "Homebrew: tint $VERSION"
          git push origin HEAD:main
